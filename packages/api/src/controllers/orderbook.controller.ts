/**
 * OrderBook REST controller (NestJS/Fastify).
 * Mirrors the Next.js API routes for the standalone backend service.
 * Routes:
 *   POST /orderbook/order   — place order
 *   DELETE /orderbook/order — cancel order
 *   GET  /orderbook/stream  — SSE stream
 */

// TODO: Implement with @nestjs/common Controller, Get, Post, Delete, Body decorators
// and inject OrderBookService

export class OrderBookController {}
